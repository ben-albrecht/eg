#include <iostream> // Required for cin/cout (part of standard library)

// We are using functions in the standard library
using namespace std;

int main()
{
  // insertion operator <<
  cout<<"Hello World!\n";

  // Wait for input before continuing
  cin.get();

  // We tell the operating system we have succeeded (1 = failed)
  return 0
}
